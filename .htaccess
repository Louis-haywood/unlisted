Options -Indexes
RewriteEngine On
RewriteBase /

# Serve real files and directories directly (including uploads)
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Everything else → index.php
RewriteRule ^(.*)$ index.php [QSA,L]
